digraph Overview {
  Pool -> ThirdParties;
  Pool -> Dev;
  Pool -> Test;
  Pool -> Prod;

  Http -> AuthService;

  Http -> Pool;

  Upload -> Http;
  API -> Http;
  LxcRepoSync -> Http;

  CLI -> API;
  CLI -> Upload;

  WebInterface -> API;
  WebInterface -> Upload;
}
